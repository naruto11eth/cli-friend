{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main Vim Helper popup window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
