{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main CLI Friend popup window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
